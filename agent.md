# Codex Agent Notes

Primary contributor guidance for this repo lives in `CLAUDE.md`.

- Follow `AGENTS.md` (repo rules) and `CLAUDE.md` (workflow + patterns).
- For commands, testing, and endpoint work instructions, use `CLAUDE.md` as the source of truth.

